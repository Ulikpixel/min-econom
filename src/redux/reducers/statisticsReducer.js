const initialState = {
    interest: [],   
};

const statisticsReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default statisticsReducer;